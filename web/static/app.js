let latestImage=null;let guideOn=true;function $(id){return document.getElementById(id)}function text(id,value){const el=$(id);if(el)el.textContent=value}async function loadStatus(){try{const r=await fetch('/api/status?ts='+Date.now());const s=await r.json();if(s.latest_image&&s.latest_image!==latestImage){latestImage=s.latest_image;refreshImage()}if(s.bme280&&s.bme280.ok){text('temp',`${s.bme280.temperature}℃`);text('hum',`${s.bme280.humidity}%`);text('press',`${s.bme280.pressure} hPa`);text('sensorMsg',`BME280 正常 bus${s.bme280.bus} ${s.bme280.address}`)}else{text('temp','未読込');text('hum','未読込');text('press','未読込');text('sensorMsg',s.bme280?.message||'BME280未接続')}text('cloud',`${s.cloud}%`);text('moon',s.moon_age);text('sqm',s.sqm);text('wind',`${s.wind?.wind_mps??'--'} m/s`);const c=s.camera||{};text('cameraInfo',`camera: rpicam-still=${c.rpicam_still} / rpicam-vid=${c.rpicam_vid}`)}catch(e){text('sensorMsg','状態取得エラー: '+e)}}function refreshImage(){if(!latestImage)return;const img=$('sky'),viewer=$('viewer'),src=`/images/${latestImage}?ts=${Date.now()}`;img.onload=()=>{img.style.display='block';viewer.classList.add('has-image');if(guideOn)viewer.classList.add('show-guide');$('savedImage').innerHTML=`<img src="${src}" alt="保存画像">`};img.onerror=()=>{img.style.display='none';viewer.classList.remove('has-image');$('empty').textContent='画像読込失敗'};img.src=src}async function capture(){text('message','撮影中...');const r=await fetch('/api/capture',{method:'POST'});const j=await r.json();if(j.ok){latestImage=j.filename;text('message',j.message);refreshImage()}else{text('message','撮影失敗: '+j.message)}}async function recordVideo(){text('message','動画撮影中...');const r=await fetch('/api/video',{method:'POST'});const j=await r.json();text('message',j.message||(j.ok?'動画保存しました':'動画失敗'))}function toggleGuide(){guideOn=!guideOn;const v=$('viewer');if(guideOn)v.classList.add('show-guide');else v.classList.remove('show-guide')}function toggleFullscreen(){const el=$('viewer');if(!document.fullscreenElement){el.requestFullscreen?.()}else{document.exitFullscreen?.()}}loadStatus();setInterval(loadStatus,10000);