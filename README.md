# Ver.3.0 Cloud AI
雲量が低く出すぎる問題を改善する、画像AI雲量版です。

GitHubへ上書き → Commit → Push → ラズパイで:
```bash
cd ~/hoshinoyakata_allsky_agent_real_v2
git pull
bash scripts/install.sh
sudo systemctl restart hoshinoyakata-allsky
```
